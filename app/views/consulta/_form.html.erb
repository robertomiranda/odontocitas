<%= form_for(@consultum) do |f| %>
  <% if @consultum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@consultum.errors.count, "error") %> prohibited this consultum from being saved:</h2>

      <ul>
      <% @consultum.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :odontologo_id %><br />
    <%= f.number_field :odontologo_id %>
  </div>
  <div class="field">
    <%= f.label :consulta_aceptada %><br />
    <%= f.text_field :consulta_aceptada %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
